{
  "name": "@agent-workbench/graph",
  "version": "0.1.0",
  "description": "Semantic code graph for AI agents - queryable knowledge base that replaces file reading",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "bin": {
    "graph-server": "dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.23.0",
    "@agent-workbench/core": "^0.1.0",
    "@agent-workbench/syntax": "^0.3.0",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.1.3"
  }
}
