# Process-Host
> AI-Agents shall never lose track of running `npm run dev` processes again.  

Back to [main README.md](../../)  

## Features
- `start_process` – runs a command and returns a session-id  
- `get_logs` – gets a sessions **last n lines** of output
- `stop_process` – terminates the running proccess via its session-id
